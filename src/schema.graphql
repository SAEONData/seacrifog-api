type Query {
  variables: [Variable!]!
  protocols: [Protocol!]!
}

# type Mutation {}

type Variable {
  id: Int
  name: String
  class: String
  domain: String
  protocols: [Protocol!]!
}

type Protocol {
  id: Int
  doi: String
  author: String
  publisher: String
  title: String
}